from setuptools import find_packages, setup

setup(
        name="reviews_classifier",
        packages=find_packages(),
        version='0.1.0',
        description='Metacritic reviews classifier"',
        )
